name: generic

on:
  push:
    branches:
      - "source"

jobs:

  gen-generic:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          fetch-depth: 0
